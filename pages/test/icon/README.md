## icon组件

### 主要属性：
```
属性名             类型              默认值             描述
type            String                          icon的类型，有效值：success,success_no_circle,info,warn,waiting,cancel,download,search,clear
size            Number              23          icon的大小(px)
color           Color                           icon的颜色，同css的color
```       